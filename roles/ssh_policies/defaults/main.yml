---
# Session/connection caps (CIS suggests MaxSessions <= 10; 4 is plenty for
# an admin box). MaxStartups throttles unauthenticated connections:
# start dropping at 10 pending, 30% probability, all beyond 60.
ssh_policies_max_sessions: 4
ssh_policies_max_startups: "10:30:60"
