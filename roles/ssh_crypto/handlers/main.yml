---
# Distinct name from the other roles' reload handlers: handler names are
# global to the play, and two roles notifying the same name is ambiguous.
- name: Reload ssh for crypto policy
  ansible.builtin.systemd:
    name: ssh
    state: reloaded
