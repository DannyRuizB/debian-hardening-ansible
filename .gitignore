# Ansible
*.retry
.vault_pass

# Local inventories with real hosts/secrets (commit inventory.ini as the example)
inventory.local.ini
host_vars/
group_vars/*.local.yml

# e2e test: throwaway SSH key/known_hosts generated by tests/node.sh
tests/.ssh_ci/
tests/.attacker/
